---
"jazz-tools": patch
---

Use wire protocol v3 for deployment-aware catalogue policy snapshots. Wire v2 peers now fail the handshake explicitly; upgrade clients, native runtimes, and Edge/Core servers together. Hosted clients require a compatible server deployment. Storage formats are unchanged and legacy stored-data upgrade paths remain supported.
