---
"jazz-tools": patch
---

Add a `runtime` client config API for explicit Wasm and worker bootstrap across browser and edge-style runtimes, including `baseUrl`, `wasmUrl`, `workerUrl`, `wasmSource`, and `wasmModule` overrides exported from the runtime and framework entrypoints.
