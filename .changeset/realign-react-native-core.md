---
"jazz-rn": patch
"jazz-tools": patch
---

Realign the React Native runtime with the current Jazz core transaction, schema-view, subscription, and session transport contracts.

React Native now supports caller-minted transaction ids across schema views, committed batch ids, typed terminal subscription operations, occurrence-key sidecars, session-bound upstream connections, and collision-resistant persistent database filenames.
