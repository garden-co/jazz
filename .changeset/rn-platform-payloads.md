---
"jazz-rn": patch
"jazz-rn-ios": patch
"jazz-rn-android": patch
---

Split React Native binaries into exact-version iOS and Android payload dependencies, retaining all supported architectures while keeping each npm upload below the package budget.
