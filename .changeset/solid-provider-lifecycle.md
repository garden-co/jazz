---
"jazz-tools": patch
---

Change Solid's `JazzProvider` to accept a reactive `config` prop and manage the client lifecycle. Use `JazzClientProvider` when passing a client created with `createSolidJazzClient`.
