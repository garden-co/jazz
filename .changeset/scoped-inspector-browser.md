---
"jazz-tools": patch
---

Add short-lived Inspector session transport and a proof-bound `jazz-tools inspect` loopback handoff. Inspector credentials use dedicated HTTP and WebSocket authentication fields and remain distinct from account JWTs and root admin secrets.
