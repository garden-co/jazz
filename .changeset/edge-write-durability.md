---
"jazz-tools": patch
---

Require accepted authority receipts and timestamps for global write waits, retain exact edge upload units until the current admitted authority settles them, and require fresh exact-subscription coverage for repeated settled reads.
