---
"jazz-tools": patch
---

Project team identifiers directly through bounded, cycle-safe reachable permission recursion frontiers without requiring membership rows for reached parents. `MaxDepth(0)` is seed-only and never admits a one-hop authorization grant.
