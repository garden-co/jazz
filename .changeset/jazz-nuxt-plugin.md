---
"jazz-tools": patch
---

Add `jazzNuxt()` Vite plugin for Nuxt projects. Automatically starts a local Jazz dev server, pushes the schema, and injects `PUBLIC_JAZZ_APP_ID` and `PUBLIC_JAZZ_SERVER_URL` into the Nuxt environment.
