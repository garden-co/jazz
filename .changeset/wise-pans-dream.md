---
"jazz-tools": patch
---

`jazz-tools server` now logs a ready-to-open inspector URL on startup using `https://jazz2-inspector.vercel.app/` with `url`, `appId`, and `adminSecret` encoded in the hash fragment.
