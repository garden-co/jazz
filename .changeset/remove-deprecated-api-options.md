---
"jazz-tools": patch
---

Remove the deprecated `authSecretStorageKey` option from browser and Expo local-first auth. Remove the deprecated `permissions` option from `publishStoredSchema`; publish permissions separately with `publishStoredPermissions`.
