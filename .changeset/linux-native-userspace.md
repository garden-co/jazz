---
"jazz-napi": patch
"jazz-tools": patch
---

Build Linux native Node packages on an AL2023-compatible userspace and publish Linux ARM64 GNU bindings alongside x64. Keep native compiler provenance and build caches tied to the selected Linux baseline.
