---
"jazz-tools": patch
---

BREAKING CHANGE: Align the Svelte `QuerySubscription` result with the other framework bindings by renaming its reactive `loading` property to `isLoading`. The idiomatic `current` property is unchanged.
