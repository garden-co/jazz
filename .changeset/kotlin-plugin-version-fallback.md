---
"jazz-rn": patch
---

Resolve the Android Kotlin Gradle plugin and standard library through the consuming React Native project's Kotlin version, with the package's configured version as the fallback.
