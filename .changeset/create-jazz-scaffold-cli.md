---
"create-jazz": patch
---

Initial release of `create-jazz`: a scaffold CLI that bootstraps a Jazz app from a starter template (currently `next-betterauth`). Resolves workspace and remote dependencies, detects the user's package manager, generates dev secrets, and runs an initial commit.
