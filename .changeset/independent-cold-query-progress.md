---
"jazz-tools": patch
---

Let ready queries advance while unrelated queries wait for storage, preserving bounded worker turns, ordered shared-node updates, and wakeups when the query owner changes.
