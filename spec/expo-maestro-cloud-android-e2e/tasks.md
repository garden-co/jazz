# Tasks

1. [ ] Add deterministic UI selectors in [`examples/todo-client-localfirst-expo/src/TodoList.tsx`](../../../examples/todo-client-localfirst-expo/src/TodoList.tsx) by introducing stable `testID` values for add input/button, per-row toggle, and per-row delete actions. (Design: Architecture / Components §7)
2. [ ] Create Maestro workspace files under `.maestro/` with an Android flow that launches the Expo app, adds multiple todos, marks one as done, deletes one, and verifies resulting visibility expectations. (Design: Architecture / Components §5, Testing Strategy “Maestro flow coverage”)
3. [ ] Add a dedicated GitHub Actions workflow file `.github/workflows/expo-android-maestro-e2e.yml` with `push` on `main` and PR path filters that include `crates/**`, `packages/jazz-tools/**`, and `examples/todo-client-localfirst-expo/**` plus lock/build plumbing files. (Design: Architecture / Components §1)
4. [ ] In the workflow, install toolchains/dependencies needed to build `jazz-tools` and Android release artifacts for the Expo example, reusing repo conventions (`pnpm`, Rust toolchain). (Design: Architecture / Components §1 and §4)
5. [ ] In the workflow, build and start `jazz-tools server` using the E2E app ID/admin secret/port, capture process output to `server.log`, and block until `/health` succeeds. (Design: Architecture / Components §2)
6. [ ] In the workflow, start an ephemeral tunnel process for `http://127.0.0.1:1625`, extract a public HTTPS URL from tunnel logs, and fail fast if URL discovery does not succeed within timeout. (Design: Architecture / Components §3)
7. [ ] In the workflow, build `examples/todo-client-localfirst-expo` with `:app:assembleRelease` while injecting `EXPO_PUBLIC_JAZZ_APP_ID`, `EXPO_PUBLIC_JAZZ_ADMIN_SECRET`, and `EXPO_PUBLIC_JAZZ_SERVER_URL` using the discovered tunnel URL. (Design: Architecture / Components §4)
8. [ ] In the workflow, run Maestro Cloud via `mobile-dev-inc/action-maestro-cloud@v2` using repository secrets `MAESTRO_KEY` and `MAESTRO_PROJECT_ID`, targeting the generated release APK and `.maestro` workspace. (Design: Architecture / Components §5)
9. [ ] Add post-run server-evidence assertions in workflow shell steps that require at least one `sync request` log line and at least one `events stream connecting` log line; fail the job if either is missing. (Design: Architecture / Components §6, Data Models `ServerEvidence`)
10. [ ] Add `if: always()` artifact upload steps for `server.log` and tunnel logs so network/sync failures can be debugged from CI artifacts. (Design: Testing Strategy “CI assertions”)
