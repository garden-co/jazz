# Type-Change Lens Generation — TODO (MVP)

Cannot auto-generate lenses for column type changes. Currently only structural changes (add/remove/rename) are supported. Workaround: manual lens creation with explicit operations.

> `crates/groove/src/schema_manager/auto_lens.rs:172-174` — TODO comment
