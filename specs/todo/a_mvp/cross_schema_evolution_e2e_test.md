# Cross-Schema Evolution E2E Test — TODO (MVP)

No RuntimeCore test for "user inserts in old schema, schema evolves, user queries new schema" — a full workflow exercising schema evolution through the public API.
