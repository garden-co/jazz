# Contribute back wasm-tracing improvements

We pulled `dsgallups/wasm-tracing` into `crates/wasm-tracing/` as a subtree and added `console.groupCollapsed`/`console.groupEnd` support for spans (opt-in via `with_console_group_spans()`).

Upstream this as a PR to https://github.com/dsgallups/wasm-tracing.
