# Jazz 1 Repo Swap — TODO

Plan the public repo swap so jazz2 becomes the main `jazz` repo while preserving stars/followers.

## Context

- This repo (`jazz2`) is currently private
- The public `jazz` repo holds Jazz 1
- Goal: make jazz2 the public `jazz` repo, move Jazz 1 to a separate public repo (e.g., `jazz-classic`)
- Must preserve GitHub stars, followers, and URL continuity on the main repo

## First-Week Tasks

- Plan the swap mechanics (GitHub repo rename/transfer)
- Set up the `jazz-classic` repo (or plan the name)
- Coordinate Discord: create Jazz 1 channel category
- Draft the one-liner for the Jazz 1 README pointing to the new repo

Full migration plan (companion post, migration tooling, community comms) is in `./jazz1_migration_plan.md`.
