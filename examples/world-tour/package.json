{
  "name": "world-tour",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx tsx scripts/dev-server.ts & vite",
    "dev:server": "npx tsx scripts/dev-server.ts",
    "dev:client": "vite",
    "build": "node ../../packages/jazz-tools/dist/cli.js build && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "jazz-tools": "workspace:*",
    "maplibre-gl": "^5.4.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
