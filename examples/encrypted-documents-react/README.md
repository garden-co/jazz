# Encrypted Documents React

Organization document vault example. PDF bytes and selected document metadata are encrypted with an organization-wide E2EE space key.

```bash
pnpm --filter encrypted-documents-react dev
```
