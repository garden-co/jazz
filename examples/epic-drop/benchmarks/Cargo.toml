[package]
name = "jazz-example-epic-drop-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jazz = { workspace = true, features = ["testing"] }
groove = { workspace = true, features = ["test"] }
jazz-benchmark-guard = { path = "../../../crates/benchmark-guard" }

[dev-dependencies]
divan = { workspace = true }

[[bench]]
name = "walltime"
harness = false
