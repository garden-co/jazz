NEXT_PUBLIC_APP_ORIGIN=http://127.0.0.1:3000
BACKEND_SECRET=replace-with-a-server-only-secret
BETTER_AUTH_SECRET=replace-with-a-better-auth-secret

# `withJazz` supplies these while running `pnpm dev`; set them only when using
# your own deployed Jazz server.
# NEXT_PUBLIC_JAZZ_APP_ID=...
# NEXT_PUBLIC_JAZZ_SERVER_URL=...
