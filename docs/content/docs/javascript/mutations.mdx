---
title: Mutations
description: Inserting, updating, and deleting data
---

Jazz provides type-safe mutations through the generated client API.

## Create

Create a new row and get its ObjectId:

<include>../../../examples/react/src/mutations.ts#create-task</include>

## Update

Update one or more fields on an existing row:

<include>../../../examples/react/src/mutations.ts#update-task</include>

## Delete

Remove a row by ID:

<include>../../../examples/react/src/mutations.ts#delete-task</include>

## Creating Related Data

When creating rows with foreign key references, pass the ObjectId:

<include>../../../examples/react/src/mutations.ts#create-related-data</include>

## With React Hooks

Use `useMutate()` to get mutation helpers in React components:

<include>../../../examples/react/src/components/UserProfile.tsx#create-user-button</include>

Or use the mutations returned from `useAll()` and `useOne()`:

<include>../../../examples/react/src/components/TaskForm.tsx#task-form</include>

## Mutations and Subscriptions

When you mutate data, all active subscriptions are automatically notified. There's no need to manually refresh queries—the reactive system handles updates automatically.
