---
title: Database Setup
description: Setting up the WASM database for JavaScript
---

Jazz uses a WASM-compiled Rust database that runs in the browser. Here's how to set it up.

## Initializing the Database

```typescript title="database.ts"
import init, { WasmDatabase } from 'groove-wasm';

export async function initDatabase() {
  // Initialize the WASM module
  await init();

  // Create an in-memory database
  const db = new WasmDatabase();

  // Initialize your schema
  db.init_schema(schemaSQL);

  return db;
}
```

## Schema Definition

Define your schema in SQL:

<include>../../../examples/react/src/schema.sql</include>

Then import and initialize:

```typescript
import schema from './schema.sql?raw';  // Vite raw import

db.init_schema(schema);
```

## Database Methods

### init_schema

Initialize the database schema:

```typescript
db.init_schema(schemaSQL);
```

### list_tables

Get all table names:

```typescript
const tables = db.list_tables();
console.log('Tables:', tables);  // ['Users', 'Projects', 'Tasks', ...]
```

## Using with the Generated Client

The generated client from `@jazz/schema` provides the type-safe API for mutations and queries. Pass the database to the client methods:

<include>../../../examples/react/src/mutations.ts#create-task</include>

## Using with React

Wrap your app with `JazzProvider`:

<include>../../../examples/react/src/App.tsx#jazz-provider-setup</include>

Then use hooks to interact with data:

<include>../../../examples/react/src/components/TaskList.tsx#task-list-basic</include>
