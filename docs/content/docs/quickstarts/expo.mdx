---
title: Expo
description: Build a local-first Expo app with Jazz React Native bindings. Schema, provider, reactive queries, and mutations.
---

import AuthMethods from "../../partials/quickstarts/auth-methods.mdx";
import AvailableColumnTypes from "../../partials/quickstarts/available-column-types.mdx";
import DbConfigReference from "../../partials/quickstarts/db-config-reference.mdx";
import FluentImmutableCallout from "../../partials/quickstarts/fluent-immutable-callout.mdx";
import NextLinks from "../../partials/quickstarts/next-links.mdx";
import PersistenceTiersTable from "../../partials/quickstarts/persistence-tiers-table.mdx";
import RunCodegen from "../../partials/quickstarts/run-codegen.mdx";
import ServerAuthConfig from "../../partials/quickstarts/server-auth-config.mdx";
import WhereOperatorsTable from "../../partials/quickstarts/where-operators-table.mdx";

## Install

Install dependencies in your Expo app:

`pnpm add jazz-tools jazz-rn expo`

## Define your schema

Create `schema/current.ts` at the root of your project. This is the source of truth for your data model.

<include cwd lang="ts" title="schema/current.ts">
  ../examples/docs/todo-client-localfirst-expo/schema/current.ts#schema-expo
</include>

### Available column types

<AvailableColumnTypes />

### Run codegen

<RunCodegen />

## Create a client and wrap your app

`JazzProvider` initializes the Jazz runtime and makes the database available to descendant components via React context.

<include cwd lang="tsx" title="src/setup-snippets.tsx">
  ../examples/docs/todo-client-localfirst-expo/src/setup-snippets.tsx#context-setup-expo-minimal
</include>

### DbConfig reference

<DbConfigReference />

For emulator/device setups, make sure `serverUrl` points to a host your runtime can reach (for example Android emulator often uses `10.0.2.2`).

## Read data with `useAll`

`useAll` subscribes to a query and returns live results that update as matching rows change.

<include cwd lang="tsx" title="src/TodoList.tsx">
  ../examples/docs/todo-client-localfirst-expo/src/TodoList.tsx#reading-reactive-hooks-expo
</include>

### Filtering, sorting, pagination

Query builders are fluent and immutable. Chain `.where()`, `.orderBy()`, `.limit()`, and `.offset()` to shape queries.

<include cwd lang="tsx" title="src/TodoList.tsx">
  ../examples/docs/todo-client-localfirst-expo/src/TodoList.tsx#reading-filtering-expo
</include>

<FluentImmutableCallout />

### Where operators

Simple equality uses a plain value. For other comparisons, pass an operator object:

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#where-operators-expo
</include>

<WhereOperatorsTable />

By default `useAll` returns results immediately from the local replica. Use settled tiers when you need explicit confirmation boundaries.

## Write data with `useDb`

`useDb` returns the `Db` handle for mutations. Mutations are synchronous at the local runtime level and sync to the server in the background.

<include cwd lang="tsx" title="src/TodoList.tsx">
  ../examples/docs/todo-client-localfirst-expo/src/TodoList.tsx#writing-use-db-expo
</include>

Because writes hit the local runtime first, subscriptions update immediately with local-first UX.

## Persistence tiers

<PersistenceTiersTable />

<Callout title="Tier support">
  `worker` is the universal baseline. Higher-tier acknowledgements depend on deployment/runtime support,
  so pick the highest tier your app path guarantees.
</Callout>

### Reading with a settled tier

Use `db.subscribeAll` (or `db.all`) with a settled tier when you need tier-gated delivery.

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#reading-tier-expo
</include>

### Writing with an ack tier

Use `*WithAck` APIs when you need explicit write acknowledgement before proceeding:

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#writing-ack-expo
</include>

## Querying in depth

The examples above use `useAll` for reactive queries in components. Jazz also exposes imperative query methods on `db` for usage outside the render cycle.

### One-shot queries

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#oneshot-expo
</include>

Outside components (or when you need deltas), use `db.subscribeAll` directly:

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#subscribe-expo
</include>

### Relations with include

If your schema has `col.ref()` columns, codegen produces typed include interfaces. Use `.include()` to load related rows in one query.

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#includes-expo
</include>

### Chaining multiple where calls

`.where()` calls are additive. Multiple calls produce AND semantics.

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#chaining-expo
</include>

### Combining everything

<include cwd lang="ts">
  ../examples/docs/todo-client-localfirst-expo/src/docs-snippets.ts#combining-expo
</include>

## Authentication

<AuthMethods />

### 1. Anonymous auth

If you provide no `jwtToken` or explicit local auth fields, Jazz uses local synthetic auth:

<include cwd lang="tsx">
  ../examples/docs/todo-client-localfirst-expo/src/auth-snippets.tsx#auth-anon-expo
</include>

If needed, you can explicitly override the local token:

<include cwd lang="tsx">
  ../examples/docs/todo-client-localfirst-expo/src/auth-snippets.tsx#auth-anon-token-expo
</include>

### 2. Demo auth (local multi-user testing)

Use demo-mode synthetic profiles for local multi-user testing:

<include cwd lang="tsx">
  ../examples/docs/todo-client-localfirst-expo/src/auth-snippets.tsx#auth-demo-expo
</include>

### 3. External auth and upgrade from local identity

After provider sign-in, link the current local identity so existing local data stays attached when you switch to external JWT sessions:

<include cwd lang="tsx">
  ../examples/docs/todo-client-localfirst-expo/src/auth-snippets.tsx#auth-external-expo
</include>

### Server auth config (`jazz-tools server`)

<ServerAuthConfig />

<include cwd lang="bash">
  ../examples/docs/todo-server-ts/docs/auth-server-cli.sh#auth-server-cli
</include>

### Offline-only mode

Omit `serverUrl` to keep all data local:

<include cwd lang="tsx">
  ../examples/docs/todo-client-localfirst-expo/src/auth-snippets.tsx#auth-offline-expo
</include>

## Next

<NextLinks />
