import { Callout } from "fumadocs-ui/components/callout";

Validate the schema and its permission policy locally:

`pnpm dlx jazz-tools@alpha validate`

This compiles `schema.ts` and the optional `permissions.ts` into the representation used by the
runtime. Run it in CI as well as before deploying a schema or policy change.
