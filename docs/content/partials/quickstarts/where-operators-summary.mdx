Use plain equality (`where({ done: true })`) or operator objects with `eq`, `ne`, `in`, `contains`, `gt`, `gte`, `lt`, `lte`, and `isNull`.

For per-column operator support, see [Reading Data: filters](/docs/reading-data#filters).
