Ensures a <code>{props.item}</code> is loaded to the specified depth.

- `options`: (Optional) An object containing:
  - `resolve`: Specifies which item references to resolve.
  - `unstable_branch`: (Optional) An unstable branch definition.

**Returns:** A promise that resolves to the resolved <code>{props.item}</code>.
