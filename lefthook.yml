pre-commit:
  commands:
    rustfmt:
      glob: "*.rs"
      run: cargo fmt
      stage_fixed: true
    clippy:
      glob: "*.rs"
      run: cargo clippy --workspace --fix --allow-dirty --allow-staged -- -D warnings
      stage_fixed: true
    oxfmt:
      glob: "*.{ts,tsx,js,jsx,md}"
      run: pnpm oxfmt .
      stage_fixed: true
    oxlint:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm oxlint --fix .
      stage_fixed: true
