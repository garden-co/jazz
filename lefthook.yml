pre-commit:
  commands:
    rustfmt:
      glob: "*.rs"
      run: cargo fmt
      stage_fixed: true
    clippy:
      glob: "*.rs"
      run: cargo clippy --workspace --fix --allow-dirty --allow-staged -- -D warnings
      stage_fixed: true
    oxfmt:
      glob: "*.{ts,tsx,js,jsx,md}"
      exclude: "crates/jazz-rn/src/**"
      run: pnpm oxfmt {staged_files}
      stage_fixed: true
    oxlint:
      glob: "*.{ts,tsx,js,jsx}"
      exclude: "crates/jazz-rn/src/**"
      run: pnpm oxlint --fix {staged_files}
      stage_fixed: true
