{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "target/**"]
    },
    "build:crates": {
      "dependsOn": ["^build:crates"],
      "outputs": ["target/**", "pkg/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
