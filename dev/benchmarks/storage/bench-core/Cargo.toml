[package]
name = "jazz-storage-bench-core"
version = "0.0.0"
edition = "2024"
publish = false
description = "Shared workload declaration, phase semantics, and engine contract for storage benchmarks."

[lib]
name = "bench_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
