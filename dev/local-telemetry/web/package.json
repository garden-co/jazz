{
  "name": "local-telemetry-viewer",
  "version": "0.0.0",
  "private": true,
  "description": "Embedded sync session log viewer for the dev/local-telemetry collector. Sources are bundled in-process by the Go binary via esbuild; this package exists for type-checking and dependency hoisting only.",
  "type": "module",
  "scripts": {
    "test": "tsc -p tsconfig.json --rootDir src --outDir .test-build && node test/sessionLogTable.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.62.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "catalog:default"
  }
}
