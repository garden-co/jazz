[package]
name = "w1-credit-core-probe"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
jazz = { path = "../../../../crates/jazz", default-features = false, features = ["testing"] }
