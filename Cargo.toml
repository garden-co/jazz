[workspace]
members = [
    "groove",
    "groove-wasm",
]
resolver = "2"

# Dev profile with debug symbols for better WASM stack traces
[profile.dev]
debug = true
opt-level = 0

[profile.release]
opt-level = "s"
lto = true


