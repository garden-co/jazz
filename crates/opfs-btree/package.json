{
  "name": "@jazz/opfs-btree",
  "private": true,
  "scripts": {
    "bench:harness:build": "env -u NO_COLOR trunk build --config wasm-bench/harness/Trunk.toml --release",
    "bench:compare": "pnpm run bench:harness:build && node wasm-bench/run-compare.cjs",
    "bench:compare:open": "env -u NO_COLOR trunk serve --config wasm-bench/harness/Trunk.toml"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
