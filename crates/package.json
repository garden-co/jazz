{
  "name": "@jazz/rust",
  "private": true,
  "scripts": {
    "build:crates": "cargo build -p jazz --bin jazz-tools --features cli",
    "test": "cargo test --workspace --lib --bins --tests --features test"
  }
}
