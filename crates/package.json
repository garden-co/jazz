{
  "name": "@jazz/rust",
  "private": true,
  "scripts": {
    "build:crates": "cargo build -p jazz-cli --bin jazz-tools",
    "test": "cargo test --workspace --lib --bins --tests --features jazz/testing,jazz/transport-compression-zstd,jazz-server/test,jazz-cli/test"
  }
}
