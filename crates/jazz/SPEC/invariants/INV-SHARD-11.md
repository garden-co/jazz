# INV-SHARD-11

- Status: open
- Coverage: untested

## Invariant

A future sharded design MUST define settled positions as per-shard positions or vectors, and MUST specify how `at(position)` and `at_time(t)` resolve across shards.

## Enforced by (tests)

NONE-FOUND

## Implementation

NONE-FOUND
