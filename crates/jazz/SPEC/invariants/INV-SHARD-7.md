# INV-SHARD-7

- Status: open
- Coverage: untested

## Invariant

A future sharded v1 MUST assign every non-global row to a schema-declared shard partition key, and MUST specify behavior for rows without a natural root and rows whose root changes.

## Enforced by (tests)

NONE-FOUND

## Implementation

NONE-FOUND
