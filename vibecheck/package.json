{
  "name": "jazz-tools-vibecheck",
  "private": true,
  "type": "module",
  "description": "Test how effectively vibe coding agents work with Jazz",
  "scripts": {
    "test:v0": "pnpm test -t '\\[v0\\]'",
    "test:claude": "pnpm test -t '\\[claude\\]'",
    "test": "ID=$(pnpm -s id) vitest run",
    "id": "node -e 'import { generateReadableId } from \"./lib/generate-readable-id.ts\"; console.log(generateReadableId())'",
    "postinstall": "npm rebuild node-pty"
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "^2.0.23",
    "@anthropic-ai/sdk": "^0.67.0",
    "@langwatch/scenario": "^0.3.0",
    "dotenv": "^17.2.3",
    "node-pty": "^1.0.0",
    "v0-sdk": "^0.14.0",
    "vitest": "catalog:default"
  }
}