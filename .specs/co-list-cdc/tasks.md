# Implementation Tasks

## Tasks

- [ ] 1. Add the public CoList CDC API and exported types on the schema returned by `co.list(...)`, including `currentCursor(...)`, `subscribeChanges(...)`, `CoListChangeCursor`, `CoListChangeSubscription`, `CoListChangeBatch`, and `CoListChange`, following the "Public API" and "Public Types" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 2. Implement `CursorCodec` with versioned cursor encoding and decoding, root ID validation, and stable resolve fingerprint normalization, following the "CDC Subsystem" and "Cursor Internals" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 3. Implement `GraphSnapshot` on top of `SubscriptionScope` so it captures reachable descendants, parent-child edges, logical path metadata, per-node `knownState`, and unresolved descendants for best-effort replay, following the "CDC Subsystem", "Reusing Existing Jazz Primitives", and "Best-Effort Descendant Failures" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 4. Implement `ReplayEngine` to merge post-cursor transactions across the active graph in global transaction order and update membership after each translated transaction, following the "Core Flow" and "Strict Historical Membership" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 5. Implement `Translator` support for `CoList`, `CoMap`, `CoPlainText`, and `CoFeed` transactions so it emits only meaningful `CoListChange` records, including synthetic `list-replace` collapse and filtering of no-op or losing writes, following the "Semantic Change Families", "Meaningful Changes Only", and "Translation Rules" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 6. Implement `ChangeSubscription` to coordinate initial graph loading, optional backlog replay, live tailing, listener delivery, cursor advancement, and descendant best-effort handling for `subscribeChanges(...)` and `currentCursor(...)`, following the "CDC Subsystem" and "Core Flow" sections of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 7. Add integration tests for root list changes, descendant `CoMap`/`CoList`/`CoPlainText`/`CoFeed` changes, replay-then-tail behavior, meaningful-change filtering, detach semantics, best-effort descendant failures, cursor incompatibility, and stable replay ordering, following the "Testing Strategy" section of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
- [ ] 8. Add focused unit tests for cursor codec behavior, resolve fingerprint normalization, graph snapshot extraction, transaction merge ordering, `list-replace` translation collapse, and `CoMap` no-op filtering, following the "Unit Test Coverage" section of [design.md](/Users/guidodorsi/workspace/jazz/.specs/co-list-cdc/design.md).
